let games = {
    "price": {

        one : 34,
        two : 30,
        three : 89,
    },

    name: "fairy tail",
}

let lengthO = Object.keys(games).length;


for (let i = 0; i < lengthO; i++) {
    console.log(this.lengthO[i]);
}